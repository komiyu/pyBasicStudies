1, open(filename_str,type)
type ==> r w a + t b

2, read(num), readline(str), readlines(list)
  write(str), writelines(list)
 
3,tell(), seek(offset,from)

4,os.rename(file,newFile), os.remove(), 
os.getcwd()
os.mkdir(dir), os.rmdir(dir),os.chdir(dir), os.listdir(dir)

